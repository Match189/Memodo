<UserControl x:Class="Memodo.Windows.Views.BoardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="14,12,14,6" Grid.Row="0">
            <TextBlock x:Name="BoardName" FontSize="18" FontWeight="Bold"
                       Foreground="#6B4A2F" VerticalAlignment="Center"/>
            <Button x:Name="PinBtn" Content="钉卡片" Margin="14,0,0,0" Padding="10,4" Click="PinBtn_Click"/>
            <CheckBox x:Name="SnapChk" Content="网格吸附 (8px)" IsChecked="True"
                      Margin="12,0,0,0" VerticalAlignment="Center" Foreground="#6B4A2F"/>
        </StackPanel>

        <Canvas x:Name="BoardCanvas" Grid.Row="1" Background="#FFD9B38C" ClipToBounds="True"/>

        <Popup x:Name="Picker" PlacementTarget="{Binding ElementName=PinBtn}"
               StaysOpen="False" AllowsTransparency="True">
            <Border Background="White" BorderBrush="#CCCCCC" BorderThickness="1"
                    Width="320" MaxHeight="440" CornerRadius="6">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="钉到图钉板" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                    <TabControl Grid.Row="1" x:Name="PickerTabs">
                        <TabItem Header="待办">
                            <ListBox x:Name="TaskList" SelectionChanged="Picker_SelectionChanged"/>
                        </TabItem>
                        <TabItem Header="备忘">
                            <ListBox x:Name="MemoList" SelectionChanged="Picker_SelectionChanged"/>
                        </TabItem>
                    </TabControl>
                    <Button Grid.Row="2" Content="关闭" Margin="0,8,0,0" Click="PickerClose_Click"/>
                </Grid>
            </Border>
        </Popup>
    </Grid>
</UserControl>
