<UserControl x:Class="Memodo.Windows.Views.MemoListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="备忘" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,14" />

        <Border Grid.Row="1" Background="{DynamicResource CardSurface}" CornerRadius="10"
                BorderBrush="{DynamicResource CardBorder}" BorderThickness="1" Margin="0,0,0,14">
            <Grid Margin="12,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="1.6*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="NewTitle" Grid.Column="0" Padding="10,8" BorderThickness="0"
                         Background="Transparent" VerticalContentAlignment="Center"
                         KeyDown="NewTitle_KeyDown" />
                <TextBox x:Name="NewContent" Grid.Column="1" Padding="10,8" Margin="8,0" BorderThickness="0"
                         Background="Transparent" VerticalContentAlignment="Center"
                         KeyDown="NewContent_KeyDown" />
                <Button Grid.Column="2" Content="添加" Padding="18,9"
                        Style="{StaticResource AccentBtn}" Click="Add_Click" />
            </Grid>
        </Border>

        <ListBox x:Name="List" Grid.Row="2" Background="Transparent" BorderThickness="0"
                 ItemsSource="{Binding Memos}"
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="12,10" Margin="0,0,0,8" CornerRadius="10"
                            Background="{DynamicResource CardSurface}" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="14" />
                                <TextBlock Text="{Binding Content}" TextWrapping="Wrap"
                                           Foreground="{DynamicResource SubtleText}"
                                           FontSize="12" Margin="0,2,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top">
                                <Button Content="&#xE70F;" FontFamily="Segoe MDL2 Assets" FontSize="13"
                                        VerticalAlignment="Top" ToolTip="编辑"
                                        Foreground="{DynamicResource Accent}" Background="Transparent" BorderThickness="0"
                                        Margin="0,0,6,0"
                                        Click="Edit_Click" Tag="{Binding Id}" />
                                <Button Content="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="13"
                                        VerticalAlignment="Top" ToolTip="删除"
                                        Foreground="{DynamicResource Danger}" Background="Transparent" BorderThickness="0"
                                        Click="Remove_Click" Tag="{Binding Id}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
