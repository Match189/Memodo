<Window x:Class="Memodo.Windows.Views.EditCardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑卡片" Width="360" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource AppBackground}">
    <StackPanel Margin="16">
        <TextBlock x:Name="KindText" Text="编辑卡片" FontWeight="SemiBold" FontSize="16" Margin="0,0,0,10"/>
        <TextBlock Text="标题" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4"/>
        <TextBox x:Name="TitleBox" Padding="8,6" Margin="0,0,0,10"/>
        <TextBlock x:Name="ContentLabel" Text="内容" Foreground="{DynamicResource TextSecondary}" Margin="0,0,0,4"/>
        <TextBox x:Name="ContentBox" Padding="8,6" Margin="0,0,0,12" MinHeight="72"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"/>

        <!-- 纸色（蓝图 §38：red/yellow/blue/green） -->
        <StackPanel x:Name="ColorPanel" Orientation="Horizontal" Margin="0,0,0,12" Visibility="Collapsed">
            <TextBlock Text="纸色" Foreground="{DynamicResource TextSecondary}"
                       VerticalAlignment="Center" Margin="0,0,8,0"/>
            <Border x:Name="ColRed"    Width="24" Height="24" CornerRadius="12" Background="#D64545" Tag="red"    Cursor="Hand" Margin="0,0,6,0" MouseLeftButtonDown="Col_Click"/>
            <Border x:Name="ColYellow" Width="24" Height="24" CornerRadius="12" Background="#E6B422" Tag="yellow" Cursor="Hand" Margin="0,0,6,0" MouseLeftButtonDown="Col_Click"/>
            <Border x:Name="ColBlue"   Width="24" Height="24" CornerRadius="12" Background="#2F7FD6" Tag="blue"   Cursor="Hand" Margin="0,0,6,0" MouseLeftButtonDown="Col_Click"/>
            <Border x:Name="ColGreen"  Width="24" Height="24" CornerRadius="12" Background="#3EA65B" Tag="green"  Cursor="Hand" Margin="0,0,6,0" MouseLeftButtonDown="Col_Click"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="取消" Padding="14,6" Click="Cancel_Click"/>
            <Button Content="保存" Padding="14,6" Margin="8,0,0,0"
                    Style="{DynamicResource AccentBtn}" Click="Save_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
