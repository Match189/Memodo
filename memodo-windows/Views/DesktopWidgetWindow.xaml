<Window x:Class="Memodo.Windows.Views.DesktopWidgetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="念念 · 桌面组件"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="CanResize"
        Width="380" Height="520" MinWidth="280" MinHeight="320">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="42" ResizeBorderThickness="8"
                      GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Border CornerRadius="16" Background="{DynamicResource GlassPanel}"
            BorderBrush="{DynamicResource GlassBorder}" BorderThickness="1"
            Margin="10" Effect="{DynamicResource DropShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <!-- 标题栏（WindowChrome 拖动区） -->
            <Border Grid.Row="0" Background="{DynamicResource Accent}" CornerRadius="16,16,0,0">
                <Grid>
                    <TextBlock Text="📌 念念" Foreground="White" FontWeight="Bold"
                               FontSize="14" Margin="14,0" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,10,0">
                        <Button x:Name="LockBtn" Content="&#xE72E;" FontFamily="Segoe MDL2 Assets"
                                Style="{StaticResource WidgetBtn}" Click="Lock_Click" ToolTip="锁定布局"/>
                        <Button Content="主窗口" Style="{StaticResource WidgetBtn}" Click="OpenMain_Click"/>
                        <Button Content="×" Style="{StaticResource WidgetBtn}" FontSize="16" Click="Close_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- 迷你 Board（§19：卡片自由摆放） -->
            <Canvas x:Name="Board" Grid.Row="1" ClipToBounds="True"
                    Background="{DynamicResource BoardBackground}" Margin="10,8"/>
            <TextBlock x:Name="EmptyHint" Grid.Row="1" Text="还没有钉住的卡片&#x0a;在下方输入即可创建并钉上"
                       TextAlignment="Center" Foreground="{DynamicResource TextSecondary}"
                       HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False"/>

            <!-- 快速添加 -->
            <Border Grid.Row="2" Background="{DynamicResource GlassPanel}" CornerRadius="0,0,16,16"
                    BorderBrush="{DynamicResource GlassBorder}" BorderThickness="0,1,0,0">
                <Grid Margin="10,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,8,0">
                        <Button x:Name="ModeTodo" Content="待办" Style="{StaticResource SegBtn}" Click="ModeTodo_Click"/>
                        <Button x:Name="ModeMemo" Content="备忘" Style="{StaticResource SegBtn}" Click="ModeMemo_Click"/>
                    </StackPanel>
                    <TextBox x:Name="AddBox" Grid.Column="1" VerticalAlignment="Center"
                             BorderThickness="0" Background="Transparent" KeyDown="AddBox_KeyDown"
                             Text="添加…" Foreground="{DynamicResource TextSecondary}"
                             GotFocus="AddBox_GotFocus" LostFocus="AddBox_LostFocus"/>
                    <Button x:Name="AddBtn" Grid.Column="2" Content="添加" Margin="8,0,0,0"
                            Style="{StaticResource AccentBtn}" Click="Add_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
