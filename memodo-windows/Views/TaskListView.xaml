<UserControl x:Class="Memodo.Windows.Views.TaskListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="待办" FontSize="22" FontWeight="SemiBold" />
            <TextBlock x:Name="CountText" DockPanel.Dock="Right" HorizontalAlignment="Right"
                       VerticalAlignment="Center" Foreground="{DynamicResource SubtleText}" />
        </DockPanel>

        <Grid Grid.Row="1" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{DynamicResource CardSurface}" CornerRadius="10"
                    BorderBrush="{DynamicResource CardBorder}" BorderThickness="1">
                <TextBox x:Name="NewTitle" Padding="12,9" BorderThickness="0" Background="Transparent"
                         VerticalContentAlignment="Center" KeyDown="NewTitle_KeyDown" />
            </Border>
            <Button Grid.Column="1" Content="添加" Margin="8,0,0,0" Padding="18,9"
                    Style="{StaticResource AccentBtn}" Click="Add_Click" />
        </Grid>

        <ListBox x:Name="List" Grid.Row="2" Background="Transparent"
                 ItemsSource="{Binding Tasks}"
                 BorderThickness="0" HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter />
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,0,8" Padding="12,10" CornerRadius="10"
                            Background="{DynamicResource CardSurface}" BorderBrush="{DynamicResource CardBorder}" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <CheckBox Grid.Column="0" IsChecked="{Binding Completed}"
                                      VerticalAlignment="Center"
                                      Click="Toggle_Click" Tag="{Binding Id}" />
                            <TextBlock Grid.Column="1" Text="{Binding Title}"
                                       FontSize="14" VerticalAlignment="Center"
                                       Margin="12,0"
                                       TextDecorations="{Binding Completed, Converter={StaticResource StrikeThrough}}" />
                            <Button Grid.Column="2" Content="&#xE74D;" FontFamily="Segoe MDL2 Assets" FontSize="13"
                                    VerticalAlignment="Center" ToolTip="删除"
                                    Foreground="{DynamicResource Danger}" Background="Transparent" BorderThickness="0"
                                    Click="Remove_Click" Tag="{Binding Id}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
