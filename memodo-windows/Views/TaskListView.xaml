<UserControl x:Class="Memodo.Windows.Views.TaskListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="待办" FontSize="22" FontWeight="SemiBold" Margin="0,0,0,12" />

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="NewTitle" Grid.Column="0" Padding="10,8" />
            <Button Grid.Column="1" Content="添加" Margin="8,0,0,0" Padding="16,8"
                    Click="Add_Click" />
        </Grid>

        <ListBox x:Name="List" Grid.Row="2"
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <CheckBox Grid.Column="0" IsChecked="{Binding Completed}"
                                  VerticalAlignment="Center"
                                  Click="Toggle_Click" Tag="{Binding Id}" />
                        <TextBlock Grid.Column="1" Text="{Binding Title}"
                                   FontSize="14" VerticalAlignment="Center"
                                   Margin="10,0"
                                   TextDecorations="{Binding Completed, Converter={StaticResource StrikeThrough}}" />
                        <Button Grid.Column="2" Content="删除"
                                Click="Remove_Click" Tag="{Binding Id}" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
